# ZAP Playwright Client

Playwright-powered crawling and OWASP browser-side checks integrated into OWASP ZAP as an add-on.

## Features

- Playwright-based crawler (React-friendly, SPA-aware)
- OWASP browser-side checks (cookies, headers, DOM XSS, JS errors)
- Integration hooks for ZAP alerts and active scans
